name: lance
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Sharing intent handling
  receive_sharing_intent: ^1.8.1
  # Printer support - Updated to flutter_pos_printer_platform_image_3
  flutter_pos_printer_platform_image_3: ^1.2.4
  esc_pos_utils: ^1.1.0
  # File handling
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  # Image handling
  image: ^3.0.2
  # Network and Bluetooth
  network_info_plus: ^4.1.0
  # Storage
  shared_preferences: ^2.2.2
  # Date formatting
  intl: ^0.19.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
